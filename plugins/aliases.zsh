alias ..="cd .."

## Colorize the ls output, use better units (e.g MB), list folders first
alias ls='ls --color=auto -h --group-directories-first'

## Use a long listing format ##
alias ll='ls -l'
alias la='ls -la'
alias l.='ls -d .* --color=auto'
